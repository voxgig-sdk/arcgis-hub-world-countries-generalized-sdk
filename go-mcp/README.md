# arcgis-hub-world-countries-generalized-mcp

MCP server exposing the ArcgisHubWorldCountriesGeneralized SDK as tools, built on the
[official Go MCP SDK](https://github.com/modelcontextprotocol/go-sdk)
and the sibling Go SDK at `../go`.

## Tools

| Tool | Args | Returns |
|------|------|---------|
| `arcgis-hub-world-countries-generalized_list` | `entity`, optional `query` map | First page of records as JSON |
| `arcgis-hub-world-countries-generalized_load` | `entity`, `query` (e.g. `{id:N}`) | Single record as JSON |

JSON schemas for both tools are emitted by the SDK from the `Args`
struct's `json` / `jsonschema` tags — no schema is hand-written.

## Entities

feature | metadata

## Build

```sh
go build -o arcgis-hub-world-countries-generalized-mcp ./...
```

## Run

```sh
# stdio transport — for spawnable agent installs (default).
./arcgis-hub-world-countries-generalized-mcp

# streamable HTTP transport — share one running server between agents.
./arcgis-hub-world-countries-generalized-mcp -transport http -addr :8080
```

## Install for Claude Code

```sh
claude mcp add --scope user arcgis-hub-world-countries-generalized \
  -- /absolute/path/to/arcgis-hub-world-countries-generalized-mcp -transport stdio
```

After install, restart Claude Code; the `arcgis-hub-world-countries-generalized_list` and
`arcgis-hub-world-countries-generalized_load` tools become available in new sessions.

## Smoke test via HTTP

```sh
./arcgis-hub-world-countries-generalized-mcp -transport http -addr :18080 &

# initialize, grab the session id
curl -sN -X POST http://localhost:18080 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -D headers \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"smoke","version":"0"}}}'

SESSION=$(awk '/Mcp-Session-Id/ {print $2}' headers | tr -d '\r')

curl -sN -X POST http://localhost:18080 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -H "Mcp-Session-Id: $SESSION" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"arcgis-hub-world-countries-generalized_load","arguments":{"entity":"feature","query":{"id":1}}}}'
```

## Generated by

sdkgen `go-mcp` target. See the target source under
`.sdk/src/cmp/go-mcp/` in this repo, or upstream at
`github.com/voxgig/sdkgen/project/.sdk/src/cmp/go-mcp/`.
